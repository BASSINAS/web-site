{
  "name": "siteweb-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx http-server ./site_web -p 3001",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.38.0"
  }
}
